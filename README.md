# ecommerce-full-ETL-process
This repository contains a full ETL workflow starting from synthetic OLTP data generation with injected data quality errors, followed by validation, logging, and transformation steps, and loading into an OLAP database. Includes a demo notebook to run and verify the entire process.
